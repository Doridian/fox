on:
  push:
    branches: [main]

name: Release

jobs:
  check:
    uses: ./.github/workflows/check.yml
    secrets: inherit
  # create_release:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #         fetch-tags: true
  #     - run: echo "RELEASE_NAME=$(./version.sh)" >> $GITHUB_ENV
  #     - name: Release mainline
  #       uses: ncipollo/release-action@v1.15.0
  #       with:
  #         name: ${{ env.RELEASE_NAME }}
  #         tag: ${{ env.RELEASE_NAME }}
  #         commit: ${{ github.sha }}
  #         allowUpdates: true
  #         artifactErrorsFailBuild: true
  #         omitBody: true
  #         omitBodyDuringUpdate: true
  #         removeArtifacts: true
  #     - uses: rickstaa/action-create-tag@v1.7.2
  #       with:
  #         tag: ${{ env.RELEASE_NAME }}
  #         force_push_tag: true
  # release:
  #   needs: [check, create_release]
  #   strategy:
  #     matrix:
  #       os:
  #         # - macos-latest
  #         - ubuntu-latest
  #         # - windows-latest
  #   runs-on: ${{ matrix.os }}
  #   steps: 
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #         fetch-tags: true
  #     - uses: actions/setup-go@v5
  #       with:
  #         go-version-file: 'go.mod'
  #         check-latest: true
  #         cache: true
